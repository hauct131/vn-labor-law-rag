-r requirements.txt

# Optional Day-4 Vietnamese sparse-retrieval experiment. Keep this separate
# because production images need a Java runtime only if this analyzer is used.
qdrant-client==1.18.0
fastembed==0.8.0
py_vncorenlp==0.1.4
pyjnius==1.7.0
