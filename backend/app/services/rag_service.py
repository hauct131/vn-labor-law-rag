"""Điều phối retrieval → context → generation → citation validation."""


class RAGService:
    async def ask(self, question: str, method: str):
        raise NotImplementedError("Sẽ triển khai ở Ngày 5–14.")
